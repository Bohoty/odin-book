import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#000',
        },
        secondary: {
            main: '#1976d2',
            light: '#4791db',
            dark: '#115293',
        }
    }
});
export default theme;